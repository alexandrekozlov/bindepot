[metadata]
name = pydemo02
version = 0.1.0
description = A minimalistic Python project using setup.cfg only
author = Your Name
license = MIT
python_requires = >=3.7

[options]
packages = find:
package_dir =
    = src
install_requires =
    requests

[options.packages.find]
where = src
